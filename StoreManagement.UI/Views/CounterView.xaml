<UserControl x:Class="StoreManagement.UI.Views.CounterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:StoreManagement.UI.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:CounterViewModel, IsDesignTimeCreatable=True}"
             Background="{DynamicResource MaterialDesignPaper}">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Title}" 
                       Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                       Margin="0,0,0,24"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            
            <materialDesign:Card Padding="32" UniformCornerRadius="12" >
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Command="{Binding DecrementCommand}" 
                            Style="{StaticResource MaterialDesignFloatingActionMiniDarkButton}"
                            Background="{DynamicResource SecondaryHueMidBrush}"
                            IsEnabled="{Binding Path=Count, Converter={StaticResource GreaterThanZeroConverter}}"
                            Margin="0,0,24,0" ToolTip="Decrement">
                        <materialDesign:PackIcon Kind="MinusThick" Width="24" Height="24" />
                    </Button>

                    <TextBlock Text="{Binding Count}" 
                               FontSize="60" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"
                               MinWidth="80" TextAlignment="Center"
                               Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                    <Button Command="{Binding IncrementCommand}" 
                            Style="{StaticResource MaterialDesignFloatingActionMiniDarkButton}"
                            Background="{DynamicResource PrimaryHueMidBrush}"
                            Margin="24,0,0,0" ToolTip="Increment">
                        <materialDesign:PackIcon Kind="PlusThick" Width="24" Height="24" />
                    </Button>
                </StackPanel>
            </materialDesign:Card>
            <TextBlock Text="Use the buttons to change the count." 
                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                       HorizontalAlignment="Center" Margin="0,16,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>