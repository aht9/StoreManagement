<UserControl x:Class="StoreManagement.WPF.Views.AddCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StoreManagement.WPF.Views"
             xmlns:viewModels="clr-namespace:StoreManagement.WPF.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:AddCustomerViewModel}"
             d:DesignHeight="500" d:DesignWidth="600">
    <Border Padding="20" MaxWidth="600">
        <StackPanel>
            <TextBlock Text="افزودن مشتری جدید" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Grid.Column="0"
                             Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="نام"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                    <TextBox Grid.Row="0" Grid.Column="2"
                             Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="نام خانوادگی"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>

                    <TextBox Grid.Row="1" Grid.Column="0" Margin="0,15,0,0"
                             Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="شماره تماس"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                    <TextBox Grid.Row="1" Grid.Column="2" Margin="0,15,0,0"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="ایمیل (اختیاری)"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>

                    <TextBox Grid.Row="2" Grid.Column="0" Margin="0,15,0,0"
                             Text="{Binding NationalCode, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="کد ملی (اختیاری)"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                    <DatePicker Grid.Row="2" Grid.Column="2" Margin="0,15,0,0"
                                Text="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged}"
                                materialDesign:HintAssist.Hint="تاریخ تولد (اختیاری)"
                                Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>

                    <TextBox Grid.Row="3" Grid.Column="0" Margin="0,15,0,0"
                             Text="{Binding City, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="شهر"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>

                    <TextBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,15,0,0"
                             Text="{Binding FullAddress, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="آدرس کامل"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Height="80"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                </Grid>
            </ScrollViewer>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Command="{Binding SaveCommand}" Content="ذخیره" IsDefault="True"/>
                <Button Command="{Binding CancelCommand}" Content="انصراف" Style="{StaticResource MaterialDesignFlatButton}" Margin="8,0,0,0" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>