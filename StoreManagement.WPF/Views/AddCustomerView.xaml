<UserControl x:Class="StoreManagement.WPF.Views.AddCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:StoreManagement.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:AddCustomerViewModel}"
             d:DesignHeight="750" d:DesignWidth="400">
    <Border Style="{StaticResource CardBorderStyle}" Width="400">
        <StackPanel FlowDirection="RightToLeft">
            <TextBlock Text="مشتری جدید" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,20"/>

            <TextBlock Text="نام" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="نشان" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="ایمیل" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="موبایل" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="شهر" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding City, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="آدرس" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding FullAddress, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="تاریخ تولد" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="کدملی" Style="{StaticResource PersianLabelStyle}" Margin="0,10,0,5"/>
            <TextBox Style="{StaticResource SearchBoxStyle}" Text="{Binding NationalCode, UpdateSourceTrigger=PropertyChanged}"/>


            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="لفو" Style="{StaticResource BaseButtonStyle}" Command="{Binding CancelCommand}" Margin="0,0,10,0"/>
                <Button Content="ذخیره" Style="{StaticResource PrimaryActionButtonStyle}" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
